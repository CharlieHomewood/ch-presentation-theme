\documentclass[aspectratio=169]{beamer}

$if(fonttheme)$
\usefonttheme{$fonttheme$}
$endif$

$for(header-includes)$
$header-includes$
$endfor$

\title{$title$}

$if(subtitle)$
\subtitle{$subtitle$}
$endif$

\author{$author$}

$if(date)$
\date{$date$}
$else$
\date{}
$endif$

\begin{document}

\begin{frame}[plain]
  \begin{center}

    \vspace{1.5cm}

    {\usebeamerfont{title}
     \usebeamercolor[fg]{title}
     \inserttitle\par}

    $if(subtitle)$
    \vspace{0.5em}

    {\usebeamerfont{subtitle}
     \usebeamercolor[fg]{subtitle}
     \insertsubtitle\par}
    $endif$

    \vspace{1em}

    {\usebeamerfont{author}
     \usebeamercolor[fg]{author}
     \insertauthor\par}

    $if(github)$
    \vspace{0.6em}

    {\small
     \href{$github$}{\texttt{$github$}}\par}
    $endif$

    $if(date)$
    \vspace{0.5em}

    {\usebeamerfont{date}
     \insertdate\par}
    $endif$

  \end{center}
\end{frame}

$body$

\end{document}